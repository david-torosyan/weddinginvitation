import { motion, AnimatePresence } from 'framer-motion';
import { useScrollLock } from '../../hooks/useScrollLock';
import { useReducedMotion } from '../../hooks/useReducedMotion';
export function InvitationGate({ open, onOpen, config }) { useScrollLock(!open); const reduced = useReducedMotion(); const initials = `${config.couple.partnerOne.slice(0, 1)}${config.couple.partnerTwo.slice(0, 1)}`; return <AnimatePresence>{!open && <motion.div className="fixed inset-0 z-50 grid place-items-center overflow-hidden bg-[#302d28] text-[#f5f0e8]" initial={{ opacity: 1 }} exit={reduced ? { opacity: 0 } : { opacity: 0, clipPath: 'inset(0 0 100% 0)' }} transition={{ duration: .85, ease: [0.76,0,0.24,1] }}><img src={config.hero.image} alt="" className="mono-image absolute inset-0 h-full w-full object-cover opacity-70"/><div className="absolute inset-0 bg-black/25"/><div className="relative flex h-full w-full max-w-[640px] flex-col items-center px-5 pb-[15vh] pt-[8vh] text-center"><p className="text-xl font-light tracking-wide sm:text-2xl">{config.couple.combinedName}</p><p aria-hidden="true" className="display mt-auto text-[clamp(10rem,45vw,18rem)] font-light leading-none text-white/65">{initials}</p><button type="button" onClick={onOpen} className="editorial-button focus-ring mt-auto w-52 border-[#f5f0e8] text-lg normal-case">{config.hero.openingButtonText}</button></div></motion.div>}</AnimatePresence>; }
