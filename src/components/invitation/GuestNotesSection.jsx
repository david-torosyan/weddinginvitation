import { AnimatedSection } from '../common/AnimatedSection'; import { Container } from '../common/Container'; import { SectionHeading } from '../common/SectionHeading';
export function GuestNotesSection({ config }) { return <AnimatedSection className="py-24 sm:py-32"><Container><div className="mx-auto max-w-3xl"><SectionHeading eyebrow="ՄԻ ՓՈՔՐ ԽՆԴՐԱՆՔ">{config.guestNotes.heading}</SectionHeading><ol className="mt-12 divide-y divide-[var(--color-border)] border-y border-[var(--color-border)]">{config.guestNotes.notes.map((note, i) => <li className="grid grid-cols-[35px_1fr] gap-4 py-6 text-sm leading-7" key={note}><span className="display text-xl text-[var(--color-accent)]">0{i + 1}</span><span>{note}</span></li>)}</ol></div></Container></AnimatedSection>; }
